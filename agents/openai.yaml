interface:
  display_name: "AllinCMS Bulk Upload"
  short_description: "Turn source files into confirmed AllinCMS site packages, then upload after live schema checks"
  default_prompt: "Use $allincms-bulk-content-upload to turn user source files into a source-backed wiki and site content package, validate confirmation/schema gates, then start read-only in AllinCMS, inspect the site or create-site flow, map the current content type, and request explicit action-time authorization before creating sites, drafts, uploads, saves, publishes, deletes, or batch operations."
